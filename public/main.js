// figure out the current page url
// find that entry in the urlPaths array
// when user clicks next button (event handler), return the urlPaths + 1 in the url
// (destinationPath = urlPaths[i] + 1)
// send user to that page

// var currentPage = req.path;
// console.log(currentPage);
// for(var i = 0; i < urlPaths.length; i++){
// 	// console.log(urlPaths[i]);
// 	if(urlPaths[i] === app.get(urlPaths[i]) ) {
// 		// ??
// 		currentPage = urlPaths[i];
// 		destinationPath = currentPage ++;
// 		return destinationPath;
// 	}
// 		// console.log(urlPaths[i]);

// }